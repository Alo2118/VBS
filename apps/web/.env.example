# Supabase (backend/dati). Valori stampati da `supabase start` o dal progetto cloud.
VITE_SUPABASE_URL=http://localhost:54321
VITE_SUPABASE_ANON_KEY=your-anon-key

# Notifiche push (opzionale finché non le attivi): chiave PUBBLICA VAPID.
# Generala con:  npx web-push generate-vapid-keys
VITE_VAPID_PUBLIC_KEY=
